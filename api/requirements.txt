# FastAPI backend — install from repo root or api/
# pip install -r api/requirements.txt

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
pydantic-settings>=2.6.0

# Persistence (PostgreSQL + Redis on Railway; SQLite + memory locally)
sqlalchemy>=2.0.36
psycopg2-binary>=2.9.10
redis>=5.2.0

# Re-use root analyzer (also needs root requirements.txt)
# pandas, yfinance, requests, google-genai, streamlit (for LLM cache decorators)
